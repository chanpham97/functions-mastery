// 1. Simple Temperature Conversion
// Define a function called 'toCelsius' that takes a temperature in Fahrenheit as a parameter and returns the equivalent temperature in Celsius.
// (Formula: (Fahrenheit - 32) * 5/9)




// Call the function with an example Fahrenheit temperature (e.g., 68) and log the result to the console.


// 2. Discount Calculator
// Define a function called 'applyDiscount' that takes two parameters: 'price' and 'discountPercentage' (e.g., 20 for 20%).
// The function should return the price after applying the discount (price - (price * discountPercentage / 100)).




// Call the function with an example price (e.g., 100) and discount percentage (e.g., 15) and log the result to the console.


// 3. Age Checker
// Define a function called 'isAdult' that takes a single parameter, 'age'.
// If the age is 18 or older, return true. Otherwise, return false.




// Call the function with an example age (e.g., 16) and log the result to see if it correctly identifies if the person is an adult.

